[project]
name = "agentic-system-design"
version = "0.1.0"
description = "Runnable companion code for the Agentic System Design book"
requires-python = ">=3.11"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]